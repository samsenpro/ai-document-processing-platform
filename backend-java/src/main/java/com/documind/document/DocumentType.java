package com.documind.document;

/** Tipos de documento que el servicio de IA sabe identificar. */
public enum DocumentType {
    INVOICE,
    CONTRACT,
    RESUME,
    RECEIPT,
    IDENTIFICATION,
    REPORT,
    OTHER
}
